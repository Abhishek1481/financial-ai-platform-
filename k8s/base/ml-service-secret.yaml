# Placeholder — empty means FakeLLMClient stays active regardless of
# ML_SERVICE_LLM_PROVIDER (see app/rag/llm_client.py: build_llm_client
# only constructs a real LangChain client when the provider isn't
# "fake"). Populate with a real key via a proper secret manager (Sealed
# Secrets, External Secrets Operator) before switching
# ml-service-configmap.yaml's ML_SERVICE_LLM_PROVIDER off "fake" —
# never commit a real key here.
apiVersion: v1
kind: Secret
metadata:
  name: ml-service-secret
  namespace: financial-ai-platform
type: Opaque
stringData:
  ML_SERVICE_LLM_API_KEY: ""
