[pytest]
# Single pytest config for the whole monorepo (tests/ is a shared top-level
# directory, not owned by any one service) so `pytest` behaves the same
# whether invoked from the repo root or from a service directory.
asyncio_mode = auto
testpaths = tests
